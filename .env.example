# Copy this file to .env and fill in real values (do NOT commit .env)
DB_HOST=localhost
DB_USER=weather_user
DB_PASSWORD=replace_with_password
DB_NAME=weathersafe_db

# Example: set `DB_HOST`, `DB_USER`, `DB_PASSWORD`, and `DB_NAME` for your environment.
# Use a strong password or passphrase (12+ characters) and consider a password manager;
# do not copy literal example passwords into production.
